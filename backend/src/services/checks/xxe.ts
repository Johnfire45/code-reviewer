import { VulnerabilityCheck } from '../../types/vulnerability';

export async function checkXXE(code: string, language: string): Promise<VulnerabilityCheck | null> {
  // TODO: Implement XXE check
  return null;
} 